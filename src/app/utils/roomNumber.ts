// import { RoomCategory } from "../module/room/room.interface";

// export function generateRoomNumber(category : RoomCategory, capacity: number , quantity: number) {
//     const roomNumbers = [];
//     const prefix = category.charAt(0); // First letter of category
//     const middle = String(capacity).padStart(2, '0'); // Capacity with padding

//     for (let i = 1; i <= quantity; i++) {
//         const suffix = String(i).padStart(2, '0'); // Sequential number with padding
//         roomNumbers.push(`${prefix}${middle}-${suffix}`);
//     }
//     return roomNumbers;
// }

